import { ListActionDirective } from './list-action.directive';

describe('ListActionDirective', () => {
    it('should create an instance', () => {
        const directive = new ListActionDirective();
        expect(directive).toBeTruthy();
    });
});

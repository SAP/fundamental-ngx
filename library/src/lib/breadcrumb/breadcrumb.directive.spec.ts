import { BreadcrumbDirective } from './breadcrumb.directive';

describe('BreadcrumbDirective', () => {
    it('should create an instance', () => {
        const directive = new BreadcrumbDirective();
        expect(directive).toBeTruthy();
    });
});

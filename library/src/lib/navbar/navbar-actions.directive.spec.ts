import { NavbarActionsDirective } from './navbar-actions.directive';

describe('NavbarActionsDirective', () => {
    it('should create an instance', () => {
        const directive = new NavbarActionsDirective();
        expect(directive).toBeTruthy();
    });
});

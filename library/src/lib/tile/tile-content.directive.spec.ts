import { TileContentDirective } from './tile-content.directive';

describe('TileContentDirective', () => {
    it('should create an instance', () => {
        const directive = new TileContentDirective();
        expect(directive).toBeTruthy();
    });
});

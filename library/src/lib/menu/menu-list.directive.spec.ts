import { MenuListDirective } from './menu-list.directive';

describe('MenuListDirective', () => {
    it('should create an instance', () => {
        const directive = new MenuListDirective();
        expect(directive).toBeTruthy();
    });
});

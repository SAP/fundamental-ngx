/*
export class Wdio {

    get phoneNumberInput(): WebdriverIO.Element {
        return $('еее');
    }

    get defaultWaitTime(): number { return browser.options.waitforTimeout }

    open(path = ''): void {
        browser.url(path);
    }

    goBack(): void {
        browser.back();
    }

    refreshPage(): void {
        browser.refresh();
    }

    click(selector: string, waitTime = this.defaultWaitTime): void {
        $(selector).waitForDisplayed();
        return $(selector).click();
    }

    setValue(selector: string, value: string, waitTime = this.defaultWaitTime): any {
        $(selector).waitForDisplayed();
        return $(selector).setValue(value);
    }
}

export const webDriver = new Wdio();
*/

{
    "$schema": "http://json-schema.org/schema",
    "$id": "add-eslint-overrides",
    "type": "object",
    "description": "Updates \".eslintrc.json\" of each project in the workspace with rule overrides for .ts files",
    "properties": {
        "project": {
            "type": "string",
            "description": "A single project to override \".eslintrc.json\" for"
        }
    },
    "required": []
}

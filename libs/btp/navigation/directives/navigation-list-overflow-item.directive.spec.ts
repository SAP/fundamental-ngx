import { NavigationListOverflowItemDirective } from './navigation-list-overflow-item.directive';

describe('NavigationListOverflowItemDirective', () => {
    it('should create an instance', () => {
        const directive = new NavigationListOverflowItemDirective();
        expect(directive).toBeTruthy();
    });
});

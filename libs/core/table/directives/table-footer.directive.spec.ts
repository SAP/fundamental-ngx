import { TableFooterDirective } from './table-footer.directive';

describe('TableFooterDirective', () => {
    it('should create an instance', () => {
        const directive = new TableFooterDirective();
        expect(directive).toBeTruthy();
    });
});

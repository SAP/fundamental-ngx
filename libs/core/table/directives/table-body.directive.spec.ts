import { TableBodyDirective } from './table-body.directive';

describe('TableBodyDirective', () => {
    it('should create an instance', () => {
        const directive = new TableBodyDirective();
        expect(directive).toBeTruthy();
    });
});

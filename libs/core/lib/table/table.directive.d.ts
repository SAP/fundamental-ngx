/**
 * The directive that represents a table.
 * A table is a set of tabular data. Line items can support data, images and actions.
 * ```html
 * <table fd-table></table>
 * ```
 */
export declare class TableDirective {
    /** @hidden */
    fdTableClass: boolean;
}

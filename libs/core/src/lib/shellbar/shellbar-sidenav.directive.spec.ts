import { ShellbarSidenavDirective } from './shellbar-sidenav.directive';

describe('ShellbarSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellbarSidenavDirective();
    expect(directive).toBeTruthy();
  });
});

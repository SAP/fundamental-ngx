import { OverflowItemsDirective } from './overflow-items.directive';

describe('OverflowItemsDirective', () => {
  it('should create an instance', () => {
    // const directive = new OverflowItemsDirective();
    // expect(directive).toBeTruthy();
  });
});

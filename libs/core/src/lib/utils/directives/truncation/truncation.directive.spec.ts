import { TruncationDirective } from './truncation.directive';

describe('TruncationDirective', () => {
    it('should create an instance', () => {
        const directive = new TruncationDirective();
        expect(directive).toBeTruthy();
    });
});

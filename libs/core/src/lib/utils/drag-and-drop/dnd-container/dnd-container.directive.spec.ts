import { DndContainerDirective } from './dnd-container.directive';

describe('DndContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new DndContainerDirective();
    expect(directive).toBeTruthy();
  });
});

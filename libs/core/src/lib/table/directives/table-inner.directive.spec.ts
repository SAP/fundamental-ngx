import { TableInnerDirective } from './table-inner.directive';

describe('TableInnerDirective', () => {
    it('should create an instance', () => {
        const directive = new TableInnerDirective();
        expect(directive).toBeTruthy();
    });
});

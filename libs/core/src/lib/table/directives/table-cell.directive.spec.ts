import { TableCellDirective } from './table-cell.directive';

describe('TableCellDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCellDirective();
    expect(directive).toBeTruthy();
  });
});

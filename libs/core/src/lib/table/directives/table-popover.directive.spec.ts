import { TablePopoverDirective } from './table-popover.directive';

describe('TablePopoverDirective', () => {
    it('should create an instance', () => {
        const directive = new TablePopoverDirective();
        expect(directive).toBeTruthy();
    });
});

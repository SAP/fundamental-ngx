import { TableTextDirective } from './table-text.directive';

describe('TableTextDirective', () => {
    it('should create an instance', () => {
        const directive = new TableTextDirective();
        expect(directive).toBeTruthy();
    });
});

import { TileContentComponent } from './tile-content.component';

describe('TileContentComponent', () => {
    it('should create an instance', () => {
        const component = new TileContentComponent();
        expect(component).toBeTruthy();
    });
});

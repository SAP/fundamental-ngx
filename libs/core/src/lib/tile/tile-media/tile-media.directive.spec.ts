import { TileMediaDirective } from './tile-media.directive';

describe('TileMediaDirective', () => {
    it('should create an instance', () => {
        const directive = new TileMediaDirective();
        expect(directive).toBeTruthy();
    });
});

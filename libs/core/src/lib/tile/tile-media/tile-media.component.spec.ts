import { TileMediaComponent } from './tile-media.component';

describe('TileMediaComponent', () => {
    it('should create an instance', () => {
        const component = new TileMediaComponent();
        expect(component).toBeTruthy();
    });
});

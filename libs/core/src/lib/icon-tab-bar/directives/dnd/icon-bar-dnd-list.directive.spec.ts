import { IconBarDndListDirective } from './icon-bar-dnd-list.directive';

describe('IconBarDndListDirective', () => {
  it('should create an instance', () => {
    const directive = new IconBarDndListDirective();
    expect(directive).toBeTruthy();
  });
});

import { ExtraButtonDirective } from './extra-button.directive';

describe('ExtraButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtraButtonDirective();
    expect(directive).toBeTruthy();
  });
});

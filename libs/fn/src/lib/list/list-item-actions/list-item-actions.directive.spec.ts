import { ListItemActionsDirective } from './list-item-actions.directive';

describe('ListItemActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ListItemActionsDirective();
    expect(directive).toBeTruthy();
  });
});

import { ListItemTitleDirective } from './list-item-title.directive';

describe('ListItemTitleDirective', () => {
    it('should create an instance', () => {
        const directive = new ListItemTitleDirective();
        expect(directive).toBeTruthy();
    });
});

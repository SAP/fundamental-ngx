import { ListItemEndDirective } from './list-item-end.directive';

describe('ListItemActionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ListItemEndDirective();
    expect(directive).toBeTruthy();
  });
});

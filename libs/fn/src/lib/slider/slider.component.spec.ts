// Angular 15 has issue with Karma and throws an error if no tests were found in the project.
// This dummy test file is used to omit the issue.
describe('SliderComponent', () => {
    it('should be true', () => {
        expect(true).toBe(true);
    });
});

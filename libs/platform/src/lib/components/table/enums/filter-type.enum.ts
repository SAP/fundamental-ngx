export enum FilterType {
    SINGLE = 'single-select',
    MULTI = 'multi-select',
    CATEGORY = 'category',
    CUSTOM = 'custom'
}

export enum FilterableColumnDataType {
    STRING = 'string',
    NUMBER = 'number',
    BOOLEAN = 'boolean',
    DATE = 'date'
}

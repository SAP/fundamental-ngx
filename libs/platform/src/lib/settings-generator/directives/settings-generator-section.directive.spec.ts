import { SettingsGeneratorSectionDirective } from './settings-generator-section.directive';

describe('SettingsGeneratorSectionDirective', () => {
    it('should create an instance', () => {
        const directive = new SettingsGeneratorSectionDirective();
        expect(directive).toBeTruthy();
    });
});

{
    "$schema": "http://json-schema.org/schema",
    "$id": "fundamental-ngx-ng-update",
    "title": "Fundamental Platform Library for Angular ng-update schematic",
    "type": "object",
    "properties": {
        "translations": {
            "type": "boolean",
            "default": true,
            "description": "Whether translations from Platform library should be added or updated.",
            "x-prompt": "Add/update translations available from Fundamental Platform Library to your project?"
        },
        "project": {
            "type": "string",
            "description": "The name of the project.",
            "$default": {
                "$source": "projectName"
            }
        }
    }
}

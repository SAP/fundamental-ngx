{
    "$schema": "http://json-schema.org/schema",
    "$id": "migrate-to-jest",
    "type": "object",
    "properties": {
        "project": {
            "type": "string",
            "description": "The name of the project"
        }
    },
    "required": ["project"]
}

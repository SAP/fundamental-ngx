{
  "$schema": "http://json-schema.org/schema",
  "id": "fundamental-ngx-ng-add",
  "title": "Fundamental Library for Angular ng-add schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "path": {
      "type": "string",
      "description": "Root path to the selected project  - a place where template files will be copied"
    },
    "moduleName": {
      "type": "string",
      "description": "Name of the module"
    },
    "addModuleAnimation": {
      "type": "boolean",
      "default": true,
      "description": "Whether Angular browser animations should be set up.",
      "x-prompt": "Add BrowserAnimationsModule?"
    },
    "addCustomStyleFile": {
      "type": "boolean",
      "default": true,
      "description": "Whether default fonts from theming should be added to custom file fundamental-styles.scss.",
      "x-prompt": "Create custom fundamental style file?"
    },
    "addAppShellModule": {
      "type": "boolean",
      "default": true,
      "description": "Tells developer if we want to add import for the AppShellModule.",
      "x-prompt": "Add AppShellModule?"
    },
    "hasModuleFederation": {
      "type": "boolean",
      "default": true,
      "description": "If we are using module federation , then main.ts is not bootstrap file. ",
      "x-prompt": "Are you using Module Federation?"
    }
  }
}

name: Update versions.json
description: Update versions.json with new version

inputs:
    url:
        description: Netlify deployment url
        required: true

runs:
    using: node20
    main: ./index.js
